{
  "_comment": "Per-visualization capture tweaks. wait = extra ms before the shot; click = selector to press first; scrollTo = selector to bring into view; viewport/scale = shoot at a different CSS size (keep width x scale = 1200 so the PNG stays 1200x900). Anything not listed uses the defaults.",
  "belt-trick-braids": {
    "viewport": { "width": 1200, "height": 900 },
    "scale": 1,
    "wait": 7000
  },
  "nets": {
    "wait": 9000
  },
  "bipartite-polyhedra": {
    "wait": 9000
  },
  "plane-curves": {
    "click": "button:has-text('PLOT')",
    "wait": 5000,
    "scrollTo": "canvas, svg"
  },
  "projective-surfaces": {
    "click": "button:has-text('PLOT')",
    "wait": 6000,
    "scrollTo": "canvas, svg"
  },
  "train-track-taffy": {
    "click": "#play",
    "wait": 14000
  }
}
